<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- regular -->
    <font
        android:font="@font/kopubdotum_normal"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/kopubdotum_normal"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <!-- italic -->
    <font
        android:font="@font/kopubdotum_light"
        android:fontStyle="italic"
        android:fontWeight="400"

        app:font="@font/kopubdotum_light"
        app:fontStyle="italic"
        app:fontWeight="400" />

    <!-- bold -->
    <font
        android:font="@font/kopubdotum_bold"
        android:fontStyle="normal"
        android:fontWeight="700"

        app:font="@font/kopubdotum_bold"
        app:fontStyle="normal"
        app:fontWeight="700" /></font-family>